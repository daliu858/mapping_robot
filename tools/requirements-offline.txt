# PC-side semantic pipeline (install into a dedicated Python 3 venv).
# PyTorch is intentionally separate because its wheel must match the GPU:
#   python -m pip install torch==2.12.0 torchvision==0.27.0 \
#     --index-url https://download.pytorch.org/whl/cu126
transformers==5.12.1
rosbags==0.11.3
numpy==2.4.4
opencv-python==4.13.0.92
Pillow==12.2.0
PyYAML==6.0.3
scipy==1.17.1
scikit-image==0.26.0
easyocr==1.7.2
